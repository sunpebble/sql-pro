{
  "name": "@quarry/cloudflare",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Quarry License API (Cloudflare Workers + D1)",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit",
    "db:create": "wrangler d1 create quarry-db",
    "db:migrate": "wrangler d1 migrations apply quarry-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply quarry-db --remote"
  },
  "dependencies": {
    "hono": "catalog:",
    "nanoid": "catalog:",
    "stripe": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
