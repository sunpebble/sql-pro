/**
 * PostgreSQL Adapter
 *
 * Re-exports the PostgreSQL adapter from the services layer.
 * This file provides a migration path to the new database module structure.
 */

export { PostgreSQLAdapter } from '../../services/database-adapters/postgresql-adapter';
