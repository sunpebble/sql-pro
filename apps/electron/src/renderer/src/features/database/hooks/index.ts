/**
 * Database Feature - Hooks
 *
 * Custom hooks for database-related functionality.
 */

// Re-export database stores as hooks
export { useConnectionStore } from '../../../stores/connection-store';
export { useConnectionSwitcherStore } from '../../../stores/connection-switcher-store';
