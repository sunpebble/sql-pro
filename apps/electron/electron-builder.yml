appId: com.sqlpro.app
productName: SQL Pro
copyright: Copyright © 2025 kunish

directories:
  buildResources: build

files:
  - from: .
    to: .
    filter:
      - package.json
      - out/**/*
      - '!out/**/*.map'
  # Include workspace root node_modules (where hoisted dependencies live)
  - from: ../../node_modules
    to: node_modules
    filter:
      - '**/*'
      - '!**/*.md'
      - '!**/*.ts'
      - '!**/*.map'
      - '!**/LICENSE*'
      - '!**/CHANGELOG*'
      - '!**/README*'
      - '!**/.github/**'
      - '!**/docs/**'
      - '!**/test/**'
      - '!**/tests/**'
      - '!**/__tests__/**'
      - '!**/example/**'
      - '!**/examples/**'
      # Exclude pnpm's virtual store (handled separately)
      - '!.pnpm/**'

# Disable asar packaging - required for sharp native module to work correctly
# Sharp's @img/* dependencies don't get properly unpacked with asarUnpack
asar: true

# Native modules that need to be unpacked from asar
# Following sharp official documentation: https://sharp.pixelplumbing.com/install/#electron
asarUnpack:
  - '**/node_modules/sharp/**/*'
  - '**/node_modules/@img/**/*'
  - '**/node_modules/better-sqlite3-multiple-ciphers/**/*'
  - '**/node_modules/@ffmpeg-installer/**/*'
  - '**/node_modules/@ffprobe-installer/**/*'

# Generate required update metadata files
generateUpdatesFilesForAllChannels: true

# macOS Configuration
mac:
  icon: resources/icon.icns
  category: public.app-category.developer-tools
  entitlementsInherit: build/entitlements.mac.plist
  notarize: false
  # Use ad-hoc signing for local builds (sign with "-")
  # For production builds, set a valid signing identity
  identity: '-'

dmg:
  artifactName: ${productName}-${version}.${ext}
  # Use ULFO for better compatibility
  format: ULFO
  writeUpdateInfo: false

# Windows Configuration
win:
  icon: resources/icon.ico

nsis:
  artifactName: ${productName}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always

# Linux Configuration
linux:
  target:
    - AppImage
    - snap
    - deb
  icon: resources/icons
  category: Development

appImage:
  artifactName: ${productName}-${version}.${ext}

npmRebuild: true

# Auto-update configuration - publish to GitHub releases
publish:
  provider: github
  owner: kunish-homelab
  repo: sql-pro
  releaseType: release
