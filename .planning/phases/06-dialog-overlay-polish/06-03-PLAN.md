---
phase: 06-dialog-overlay-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/card.tsx
autonomous: true

must_haves:
  truths:
    - 'Card hover state uses background change only, no scale or shadow lift'
    - 'Skeleton component uses minimal single-color pulse animation'
    - 'Spinner component uses minimal spin animation'
  artifacts:
    - path: 'packages/ui/src/card.tsx'
      provides: 'Card without hover scale/shadow'
      contains: 'transition'
    - path: 'packages/ui/src/skeleton.tsx'
      provides: 'Minimal skeleton component'
      contains: 'animate-pulse'
    - path: 'packages/ui/src/spinner.tsx'
      provides: 'Minimal spinner component'
      contains: 'animate-spin'
  key_links:
    - from: 'Card'
      to: 'Tailwind hover utilities'
      via: 'className'
      pattern: 'hover:ring-primary'
---

<objective>
Standardize Card hover states (remove scale/shadow lift) and verify loading states are minimal.

Purpose: Prior decisions established background-only hover states (no scale/lift effects). The Card component still has `hover:scale-[1.01] hover:shadow-md` which conflicts with this pattern. Skeleton and Spinner are already minimal but should be verified. This addresses VISL-05 and VISL-06 requirements.

Output: Card component with subtle hover state, verified minimal loading components.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dialog-overlay-polish/06-RESEARCH.md

@packages/ui/src/card.tsx
@packages/ui/src/skeleton.tsx
@packages/ui/src/spinner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove scale and shadow hover from Card</name>
  <files>packages/ui/src/card.tsx</files>
  <action>
In Card component (around line 14-16), modify the className:

REMOVE these classes:

- `hover:scale-[1.01]`
- `hover:shadow-md`
- `transition-all` (change to `transition-colors` for ring color transition)

KEEP these hover classes:

- `hover:ring-primary/20` (subtle ring color change on hover)

The result should change from:

```
'ring-foreground/10 bg-card text-card-foreground group/card hover:ring-primary/20 flex flex-col gap-6 overflow-hidden rounded-xl py-6 text-sm shadow-xs ring-1 transition-all duration-200 ease-out hover:scale-[1.01] hover:shadow-md has-[>img:first-child]:pt-0...'
```

To:

```
'ring-foreground/10 bg-card text-card-foreground group/card hover:ring-primary/20 flex flex-col gap-6 overflow-hidden rounded-xl py-6 text-sm shadow-xs ring-1 transition-colors duration-200 ease-out has-[>img:first-child]:pt-0...'
```

WHY: Per Phase 4/5 decisions, hover states should be subtle background/ring changes only. Scale and shadow lift effects are too playful for a professional app. The ring color change provides sufficient feedback.
</action>
<verify>
grep -v "hover:scale" packages/ui/src/card.tsx | grep -q "hover:ring-primary"
grep -v "hover:shadow-md" packages/ui/src/card.tsx | grep -q "transition-colors"
</verify>
<done>Card no longer has hover:scale or hover:shadow-md, uses transition-colors</done>
</task>

<task type="auto">
  <name>Task 2: Verify Skeleton and Spinner are minimal</name>
  <files>packages/ui/src/skeleton.tsx, packages/ui/src/spinner.tsx</files>
  <action>
VERIFICATION ONLY - No changes expected.

**skeleton.tsx** - Verify it contains:

- `bg-muted` (single color, no gradient)
- `animate-pulse` (simple animation)
- `rounded-md` (soft corners)

**spinner.tsx** - Verify it contains:

- `animate-spin` (simple rotation)
- Uses `Loader2Icon` from lucide-react

If either file has shimmer gradients, complex animations, or decorative effects, simplify to the minimal pattern above.

Document verification results in the summary.
</action>
<verify>
grep "bg-muted" packages/ui/src/skeleton.tsx
grep "animate-pulse" packages/ui/src/skeleton.tsx
grep "animate-spin" packages/ui/src/spinner.tsx
</verify>
<done>Skeleton uses bg-muted + animate-pulse, Spinner uses animate-spin - both minimal</done>
</task>

</tasks>

<verification>
1. Run TypeScript check: `pnpm tsc --noEmit -p packages/ui/tsconfig.json`
2. Card has no `hover:scale` or `hover:shadow-md`
3. Card uses `transition-colors` instead of `transition-all`
4. Skeleton has `bg-muted animate-pulse`
5. Spinner has `animate-spin`
</verification>

<success_criteria>

- Card hover state is background/ring only (no scale, no shadow lift)
- Card uses `transition-colors` for smooth ring color transition
- Skeleton is minimal (single color + pulse)
- Spinner is minimal (single icon + spin)
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/06-dialog-overlay-polish/06-03-SUMMARY.md`
</output>
