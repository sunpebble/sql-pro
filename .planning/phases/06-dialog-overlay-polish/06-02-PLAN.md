---
phase: 06-dialog-overlay-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/popover.tsx
  - packages/ui/src/dropdown-menu.tsx
  - packages/ui/src/context-menu.tsx
  - packages/ui/src/tooltip.tsx
  - packages/ui/src/hover-card.tsx
autonomous: true

must_haves:
  truths:
    - 'Popover content displays subtle frosted glass effect'
    - 'Dropdown menu content displays subtle frosted glass effect'
    - 'Context menu content displays subtle frosted glass effect'
    - 'Tooltip content displays subtle frosted glass effect'
    - 'Hover card content displays subtle frosted glass effect'
  artifacts:
    - path: 'packages/ui/src/popover.tsx'
      provides: 'PopoverContent with glassmorphism'
      contains: 'backdrop-blur'
    - path: 'packages/ui/src/dropdown-menu.tsx'
      provides: 'DropdownMenuContent with glassmorphism'
      contains: 'backdrop-blur'
    - path: 'packages/ui/src/context-menu.tsx'
      provides: 'ContextMenuContent with glassmorphism'
      contains: 'backdrop-blur'
    - path: 'packages/ui/src/tooltip.tsx'
      provides: 'TooltipContent with glassmorphism'
      contains: 'backdrop-blur'
    - path: 'packages/ui/src/hover-card.tsx'
      provides: 'HoverCardContent with glassmorphism'
      contains: 'backdrop-blur'
  key_links:
    - from: 'PopoverContent'
      to: 'Tailwind backdrop-blur-md'
      via: 'className'
      pattern: 'bg-popover/90.*backdrop-blur-md'
    - from: 'DropdownMenuContent'
      to: 'Tailwind backdrop-blur-md'
      via: 'className'
      pattern: 'bg-popover/90.*backdrop-blur-md'
---

<objective>
Apply glassmorphism effect to popover-type overlay content panels (Popover, DropdownMenu, ContextMenu, Tooltip, HoverCard).

Purpose: All floating overlay panels should have consistent premium glassmorphism styling. These smaller overlays use the same pattern as dialogs but with appropriately sized blur for performance. This addresses VISL-04 requirement.

Output: All popover-type content components will have semi-transparent backgrounds with backdrop blur.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dialog-overlay-polish/06-RESEARCH.md

@packages/ui/src/popover.tsx
@packages/ui/src/dropdown-menu.tsx
@packages/ui/src/context-menu.tsx
@packages/ui/src/tooltip.tsx
@packages/ui/src/hover-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glassmorphism to PopoverContent and DropdownMenuContent</name>
  <files>packages/ui/src/popover.tsx, packages/ui/src/dropdown-menu.tsx</files>
  <action>
**popover.tsx** - In PopoverContent (around line 86-91):
Change `bg-popover text-popover-foreground` to `bg-popover/90 backdrop-blur-md text-popover-foreground`

**dropdown-menu.tsx** - In DropdownMenuContent (around line 55-58):
Change `bg-popover text-popover-foreground` to `bg-popover/90 backdrop-blur-md text-popover-foreground`

Also update DropdownMenuSubContent (around line 159-161):
Change `bg-popover text-popover-foreground` to `bg-popover/90 backdrop-blur-md text-popover-foreground`

Keep all other classes intact (animations, ring, shadow, etc.)

WHY: These are the most commonly used overlays in the app. Consistent glassmorphism creates premium feel.
</action>
<verify>
grep "backdrop-blur-md" packages/ui/src/popover.tsx
grep "backdrop-blur-md" packages/ui/src/dropdown-menu.tsx
</verify>
<done>PopoverContent and DropdownMenuContent (including SubContent) have glassmorphism</done>
</task>

<task type="auto">
  <name>Task 2: Add glassmorphism to ContextMenuContent</name>
  <files>packages/ui/src/context-menu.tsx</files>
  <action>
In ContextMenuContent (around line 51-55):
Change `bg-popover text-popover-foreground` to `bg-popover/90 backdrop-blur-md text-popover-foreground`

ContextMenuSubContent delegates to ContextMenuContent so it will inherit the change.

Keep all other classes intact.

WHY: Context menus appear on right-click and should match dropdown menu styling for consistency.
</action>
<verify>
grep "backdrop-blur-md" packages/ui/src/context-menu.tsx
</verify>
<done>ContextMenuContent has glassmorphism</done>
</task>

<task type="auto">
  <name>Task 3: Add glassmorphism to TooltipContent and HoverCardContent</name>
  <files>packages/ui/src/tooltip.tsx, packages/ui/src/hover-card.tsx</files>
  <action>
**tooltip.tsx** - In TooltipContent (around line 98-101):
Change `'bg-popover text-popover-foreground border-border/60 border shadow-lg'` to
`'bg-popover/95 backdrop-blur-sm text-popover-foreground border-border/60 border shadow-lg'`

Note: Tooltips use lighter blur (backdrop-blur-sm) and higher opacity (95%) because they are smaller and need to remain crisp.

**hover-card.tsx** - In HoverCardContent (around line 40-42):
Change `bg-popover text-popover-foreground` to `bg-popover/90 backdrop-blur-md text-popover-foreground`

Keep all other classes intact.

WHY: These ephemeral overlays benefit from subtle glassmorphism. Tooltips are smaller so use lighter blur for crispness.
</action>
<verify>
grep "backdrop-blur" packages/ui/src/tooltip.tsx
grep "backdrop-blur-md" packages/ui/src/hover-card.tsx
</verify>
<done>TooltipContent and HoverCardContent have glassmorphism</done>
</task>

</tasks>

<verification>
1. Run TypeScript check: `pnpm tsc --noEmit -p packages/ui/tsconfig.json`
2. All 5 files contain `backdrop-blur` in their content components
3. All 5 files use `/90` or `/95` opacity on background color
</verification>

<success_criteria>

- PopoverContent uses `bg-popover/90 backdrop-blur-md`
- DropdownMenuContent uses `bg-popover/90 backdrop-blur-md`
- DropdownMenuSubContent uses `bg-popover/90 backdrop-blur-md`
- ContextMenuContent uses `bg-popover/90 backdrop-blur-md`
- TooltipContent uses `bg-popover/95 backdrop-blur-sm`
- HoverCardContent uses `bg-popover/90 backdrop-blur-md`
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/06-dialog-overlay-polish/06-02-SUMMARY.md`
</output>
