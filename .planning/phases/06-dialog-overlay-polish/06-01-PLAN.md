---
phase: 06-dialog-overlay-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/alert-dialog.tsx
  - packages/ui/src/sheet.tsx
autonomous: true

must_haves:
  truths:
    - 'AlertDialog content displays subtle frosted glass effect behind it'
    - 'Sheet content displays subtle frosted glass effect behind it'
    - 'Dialog content remains readable with proper contrast'
  artifacts:
    - path: 'packages/ui/src/alert-dialog.tsx'
      provides: 'AlertDialogContent with glassmorphism'
      contains: 'backdrop-blur'
    - path: 'packages/ui/src/sheet.tsx'
      provides: 'SheetContent with glassmorphism'
      contains: 'backdrop-blur'
  key_links:
    - from: 'AlertDialogContent'
      to: 'Tailwind backdrop-blur-md utility'
      via: 'className prop'
      pattern: 'bg-background/90.*backdrop-blur'
    - from: 'SheetContent'
      to: 'Tailwind backdrop-blur-md utility'
      via: 'className prop'
      pattern: 'bg-background/90.*backdrop-blur'
---

<objective>
Apply glassmorphism effect to dialog-type overlay content panels (AlertDialog, Sheet).

Purpose: These full-screen modal dialogs should feel premium with subtle backdrop blur, creating depth and focus on the dialog content. This addresses VISL-04 requirement.

Output: AlertDialogContent and SheetContent will have semi-transparent backgrounds with backdrop blur effect.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dialog-overlay-polish/06-RESEARCH.md

@packages/ui/src/alert-dialog.tsx
@packages/ui/src/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glassmorphism to AlertDialogContent</name>
  <files>packages/ui/src/alert-dialog.tsx</files>
  <action>
In AlertDialogContent (around line 56-60), modify the className:

1. Change `bg-background` to `bg-background/90 backdrop-blur-md`
2. Keep all existing classes intact (ring-1, ring-foreground/10, rounded-xl, etc.)

The result should be:

```tsx
className={cn(
  'data-open:animate-in data-closed:animate-out data-closed:fade-out-0 data-open:fade-in-0 data-closed:zoom-out-95 data-open:zoom-in-95 bg-background/90 backdrop-blur-md ring-foreground/10 group/alert-dialog-content fixed top-1/2 left-1/2 z-50 grid w-full -translate-x-1/2 -translate-y-1/2 gap-6 rounded-xl p-6 ring-1 duration-100 outline-none data-[size=default]:max-w-xs data-[size=sm]:max-w-xs data-[size=default]:sm:max-w-lg',
  className
)}
```

WHY: Semi-transparent background (90% opacity) with blur creates premium frosted glass effect while maintaining readability.
</action>
<verify>
Run TypeScript check: `pnpm tsc --noEmit -p packages/ui/tsconfig.json`
Verify classes in file: grep for "backdrop-blur-md" in alert-dialog.tsx
</verify>
<done>AlertDialogContent has bg-background/90 backdrop-blur-md classes</done>
</task>

<task type="auto">
  <name>Task 2: Add glassmorphism to SheetContent</name>
  <files>packages/ui/src/sheet.tsx</files>
  <action>
In SheetContent (around line 59-63), modify the className:

1. Change `bg-background` to `bg-background/90 backdrop-blur-md`
2. Keep all existing classes intact (animations, positioning, borders, etc.)

Find the line with `'bg-background data-open:animate-in` and change to `'bg-background/90 backdrop-blur-md data-open:animate-in`

WHY: Sheet panels slide in from edges and benefit from the same premium feel as dialogs.
</action>
<verify>
Run TypeScript check: `pnpm tsc --noEmit -p packages/ui/tsconfig.json`
Verify classes in file: grep for "backdrop-blur-md" in sheet.tsx
</verify>
<done>SheetContent has bg-background/90 backdrop-blur-md classes</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. Both files contain `backdrop-blur-md` in their content components
3. Both files use `/90` opacity on background color
</verification>

<success_criteria>

- AlertDialogContent uses `bg-background/90 backdrop-blur-md`
- SheetContent uses `bg-background/90 backdrop-blur-md`
- No TypeScript errors
- Existing animations and positioning preserved
  </success_criteria>

<output>
After completion, create `.planning/phases/06-dialog-overlay-polish/06-01-SUMMARY.md`
</output>
