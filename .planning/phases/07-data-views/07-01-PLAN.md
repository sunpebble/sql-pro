---
phase: 07-data-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/electron/src/renderer/src/components/data-table/TableBody.tsx
  - apps/electron/src/renderer/src/components/data-table/TableCell.tsx
autonomous: true

must_haves:
  truths:
    - 'User sees row highlight when hovering any table row'
    - 'Selected rows show enhanced highlight on hover'
    - 'Pinned columns maintain consistent hover state with rest of row'
    - 'Hover transitions feel smooth (100ms ease-out)'
  artifacts:
    - path: 'apps/electron/src/renderer/src/components/data-table/TableBody.tsx'
      provides: 'Row hover states with group class'
      contains: 'hover:bg-muted/50'
    - path: 'apps/electron/src/renderer/src/components/data-table/TableCell.tsx'
      provides: 'Pinned cell group-hover inheritance'
      contains: 'group-hover:bg-muted/50'
  key_links:
    - from: 'TableBody.tsx (DataRow tr)'
      to: 'TableCell.tsx (pinned td)'
      via: 'Tailwind group/group-hover pattern'
      pattern: 'group.*hover:bg-muted'
---

<objective>
Add row hover states to the data table for clear visual feedback at high data density (500+ rows).

Purpose: Users need to track which row their cursor is on when scanning large result sets. Currently zebra striping exists but no hover feedback.
Output: TableBody.tsx and TableCell.tsx updated with hover states following established patterns (bg-muted/50 hover, 100ms transitions).
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-views/07-RESEARCH.md

# Source files

@apps/electron/src/renderer/src/components/data-table/TableBody.tsx
@apps/electron/src/renderer/src/components/data-table/TableCell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add row hover states to DataRow</name>
  <files>apps/electron/src/renderer/src/components/data-table/TableBody.tsx</files>
  <action>
Update the DataRow component's rowClassName (around line 237):

1. Add `group` class to enable group-hover on child elements (pinned cells)
2. Add `transition-colors duration-100` for smooth hover feedback (per STATE.md pattern)
3. Add `hover:bg-muted/50` for base hover state
4. Add `hover:bg-primary/15` specifically for selected rows (enhances bg-primary/10)

The updated rowClassName should be:

```tsx
const rowClassName = cn(
  'group border-border h-6 border-b',
  'transition-colors duration-100',
  isEven ? 'bg-background' : 'bg-muted/20',
  'hover:bg-muted/50',
  isDeleted && 'bg-destructive/10 line-through opacity-50',
  isNewRow && 'bg-green-500/10',
  isSelected && 'bg-primary/10 hover:bg-primary/15',
  isInDragRange && !isSelected && 'bg-primary/5'
);
```

Note: The `group` class is essential for Task 2 (pinned column hover inheritance).
</action>
<verify>

1. Build passes: `pnpm --filter @sqlpro/electron build:renderer`
2. Manual check: Hover over data table rows and confirm highlight appears
3. Confirm selected rows show slightly brighter hover (primary/15 vs primary/10)
   </verify>
   <done>

- Row hover shows bg-muted/50 highlight
- Selected row hover shows bg-primary/15 highlight
- Transition is smooth (100ms)
  </done>
  </task>

<task type="auto">
  <name>Task 2: Add group-hover to pinned cells and selection column</name>
  <files>
apps/electron/src/renderer/src/components/data-table/TableBody.tsx
apps/electron/src/renderer/src/components/data-table/TableCell.tsx
  </files>
  <action>
Pinned columns use `bg-background` to mask scroll content behind them. They need `group-hover` to inherit the row's hover state.

**In TableBody.tsx** - Update the selection cell (around line 261):
Change:

```tsx
<td className="bg-background sticky left-0 z-10 cursor-default border-r px-2 select-none"
```

To:

```tsx
<td className="bg-background group-hover:bg-muted/50 sticky left-0 z-10 cursor-default border-r px-2 select-none transition-colors duration-100"
```

**In TableCell.tsx** - Update the pinnedClassName (around line 259):
Change:

```tsx
const pinnedClassName = cn(isPinned && 'bg-background', isLastPinned && '...');
```

To:

```tsx
const pinnedClassName = cn(
  isPinned &&
    'bg-background group-hover:bg-muted/50 transition-colors duration-100',
  isLastPinned && '...'
);
```

This ensures pinned columns don't show a "gap" in the hover highlight.
</action>
<verify>

1. Build passes: `pnpm --filter @sqlpro/electron build:renderer`
2. Manual check: With pinned columns, hover should show consistent highlight across entire row
3. Selection checkbox column (if visible) should also highlight on row hover
   </verify>
   <done>

- Pinned columns show hover highlight matching row
- Selection column shows hover highlight matching row
- No visual "gap" at pinned column boundaries
  </done>
  </task>

</tasks>

<verification>
After both tasks complete:

1. **Build verification**: `pnpm --filter @sqlpro/electron build:renderer` succeeds
2. **Visual verification**:
   - Open the app with a table containing data
   - Hover over rows - confirm bg-muted/50 highlight appears
   - Select a row - confirm hover shows brighter bg-primary/15
   - If columns are pinned, confirm hover extends through pinned area
3. **Performance check**: Scroll through 500+ rows - should remain smooth (CSS containment already in place)
   </verification>

<success_criteria>

1. Table rows remain distinguishable at 500+ row result sets (zebra striping preserved)
2. Row hover states provide clear visual feedback (bg-muted/50)
3. Selected rows show enhanced hover feedback (bg-primary/15)
4. Pinned columns maintain hover consistency with rest of row
5. Transitions complete in 100ms (matching STATE.md pattern)
   </success_criteria>

<output>
After completion, create `.planning/phases/07-data-views/07-01-SUMMARY.md`
</output>
