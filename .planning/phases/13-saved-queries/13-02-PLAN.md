---
phase: 13-saved-queries
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/electron/src/renderer/src/components/saved-queries/SaveQueryDialog.tsx
  - apps/electron/src/renderer/src/components/saved-queries/EditQueryDialog.tsx
  - apps/electron/src/renderer/src/components/saved-queries/ParameterInputDialog.tsx
  - apps/electron/src/renderer/src/components/saved-queries/SavedQueriesBrowser.tsx
  - apps/electron/src/renderer/src/components/saved-queries/QueryCard.tsx
  - apps/electron/src/renderer/src/components/saved-queries/FolderManagement.tsx
  - apps/electron/src/renderer/src/components/saved-queries/index.ts
autonomous: true

must_haves:
  truths:
    - 'User can see save query dialog with name, description, folder fields'
    - 'User can browse saved queries in a dialog with folder sidebar'
    - 'User can enter parameter values when running parameterized query'
    - 'Query cards show name, description, folder, and action buttons'
  artifacts:
    - path: 'apps/electron/src/renderer/src/components/saved-queries/SaveQueryDialog.tsx'
      provides: 'Dialog to save current query'
      min_lines: 80
    - path: 'apps/electron/src/renderer/src/components/saved-queries/ParameterInputDialog.tsx'
      provides: 'Dialog to enter parameter values'
      min_lines: 60
    - path: 'apps/electron/src/renderer/src/components/saved-queries/SavedQueriesBrowser.tsx'
      provides: 'Main browser dialog with folder sidebar'
      min_lines: 120
    - path: 'apps/electron/src/renderer/src/components/saved-queries/index.ts'
      provides: 'Barrel exports'
      exports:
        [
          'SaveQueryDialog',
          'EditQueryDialog',
          'ParameterInputDialog',
          'SavedQueriesBrowser',
        ]
  key_links:
    - from: 'SaveQueryDialog.tsx'
      to: 'saved-queries-store'
      via: 'useSavedQueriesStore import'
      pattern: 'useSavedQueriesStore'
---

<objective>
Create all UI components for the saved queries feature.

Purpose: Provide dialogs and browsing interface for saving, viewing, and running queries.
Output: SaveQueryDialog, EditQueryDialog, ParameterInputDialog, SavedQueriesBrowser, and supporting components.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-saved-queries/13-RESEARCH.md

# Reference existing dialog patterns

@apps/electron/src/renderer/src/components/query-editor/QueryTemplatesPicker.tsx
@apps/electron/src/renderer/src/components/tags/TagDialog.tsx
@apps/electron/src/renderer/src/components/tags/ColorPicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SaveQueryDialog and EditQueryDialog</name>
  <files>
apps/electron/src/renderer/src/components/saved-queries/SaveQueryDialog.tsx
apps/electron/src/renderer/src/components/saved-queries/EditQueryDialog.tsx
  </files>
  <action>
**SaveQueryDialog.tsx:**
Props: `{ open, onOpenChange, initialQuery: string }`

UI elements:

- Dialog with title "Save Query"
- Input for name (required)
- Input for description (optional)
- Select dropdown for folder (show "No folder" + existing folders)
- If query has {{parameters}}, show detected parameters as badges
- Cancel and Save buttons

Behavior:

- On save: call useSavedQueriesStore().saveQuery()
- Show toast on success
- Reset form and close dialog

**EditQueryDialog.tsx:**
Props: `{ open, onOpenChange, query: SavedQuery | null }`

Same UI as SaveQueryDialog but:

- Pre-populate fields from query prop
- Include a Monaco editor (read-only preview or editable)
- On save: call updateQuery() instead of saveQuery()
- Add delete button with confirmation

Follow TagDialog.tsx pattern for dialog structure.
Use shadcn Dialog, Input, Label, Button, Select components.
Use useTranslation for i18n (add keys to translation files later).
</action>
<verify>Files exist and TypeScript compiles: `pnpm typecheck`</verify>
<done>SaveQueryDialog and EditQueryDialog components render correctly with all form fields</done>
</task>

<task type="auto">
  <name>Task 2: Create ParameterInputDialog</name>
  <files>apps/electron/src/renderer/src/components/saved-queries/ParameterInputDialog.tsx</files>
  <action>
Props:
```typescript
interface ParameterInputDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  parameters: QueryParameter[];
  queryName: string;
  onSubmit: (values: Record<string, string>) => void;
}
```

UI:

- Dialog title: "Enter Parameters"
- Description: "Provide values for '{queryName}'"
- For each parameter:
  - Label with parameter name
  - If type !== 'string', show type hint in muted text
  - Input field (type="number" for number type, otherwise text)
  - Placeholder shows defaultValue or "Enter {name}"
- Cancel and "Run Query" buttons

State:

- Initialize values Record from parameter defaultValues
- Update values on input change

On submit:

- Call onSubmit with values
- Close dialog
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>ParameterInputDialog renders input fields for each parameter with type-appropriate inputs</done>
  </task>

<task type="auto">
  <name>Task 3: Create SavedQueriesBrowser with folder sidebar</name>
  <files>
apps/electron/src/renderer/src/components/saved-queries/SavedQueriesBrowser.tsx
apps/electron/src/renderer/src/components/saved-queries/QueryCard.tsx
apps/electron/src/renderer/src/components/saved-queries/FolderManagement.tsx
apps/electron/src/renderer/src/components/saved-queries/index.ts
  </files>
  <action>
**QueryCard.tsx:**
Props: `{ query: SavedQuery, onSelect, onRun, onEdit, onDelete }`

Display:

- Card with hover effect (follow existing card patterns)
- Query name as title
- Description in muted text (truncated)
- Execution count badge if > 0
- Action buttons: Play (run), Pencil (edit), Trash (delete)
- If query has parameters, show small "Has parameters" badge

**FolderManagement.tsx:**
Props: `{ onCreateFolder }`

Simple inline form for creating new folders:

- Input field + Add button
- Optional color picker (reuse ColorPicker from tags)

**SavedQueriesBrowser.tsx:**
Props:

```typescript
{
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSelect: (query: SavedQuery) => void;  // Load into editor
  onRun: (query: SavedQuery) => void;     // Execute query
}
```

Layout (follow QueryTemplatesPicker pattern):

- Large dialog (max-w-4xl)
- Header with title and FileText icon
- Left sidebar (w-48):
  - "All Queries" button (active when activeFolderId === null)
  - List of folders with FolderOpen icon
  - FolderManagement component at bottom
- Main content area:
  - Search input at top
  - Grid of QueryCards (grid-cols-2)
  - ScrollArea for overflow
  - Empty state when no queries match

Use cn() for conditional classes.
Use ScrollArea from shadcn.

**index.ts:**
Barrel export all components:

```typescript
export { SaveQueryDialog } from './SaveQueryDialog';
export { EditQueryDialog } from './EditQueryDialog';
export { ParameterInputDialog } from './ParameterInputDialog';
export { SavedQueriesBrowser } from './SavedQueriesBrowser';
export { QueryCard } from './QueryCard';
export { FolderManagement } from './FolderManagement';
```

  </action>
  <verify>
All files exist and compile: `pnpm typecheck`
Index exports all components: `grep "export" apps/electron/src/renderer/src/components/saved-queries/index.ts`
  </verify>
  <done>
- SavedQueriesBrowser shows folder sidebar + query grid
- QueryCard displays query info with action buttons
- FolderManagement allows creating new folders
- All components exported from index.ts
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `pnpm typecheck`
2. All component files exist in saved-queries folder
3. Index.ts exports all public components
4. Components follow existing dialog patterns (TagDialog, QueryTemplatesPicker)
</verification>

<success_criteria>

- SaveQueryDialog allows saving current query with name/description/folder
- EditQueryDialog allows editing existing query properties
- ParameterInputDialog shows inputs for each {{variable}}
- SavedQueriesBrowser provides folder navigation and query grid
- All components use shadcn/ui and follow project design system
  </success_criteria>

<output>
After completion, create `.planning/phases/13-saved-queries/13-02-SUMMARY.md`
</output>
