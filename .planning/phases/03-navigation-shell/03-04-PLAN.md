---
phase: 03-navigation-shell
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/electron/src/renderer/src/components/Sidebar.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - 'Filter/manage tabs use pill styling consistent with other tab bars'
  artifacts:
    - path: 'apps/electron/src/renderer/src/components/Sidebar.tsx'
      provides: 'Pill-styled filter/manage tabs'
      contains: 'bg-accent text-foreground rounded-md'
  key_links:
    - from: 'Sidebar.tsx filter/manage tabs'
      to: 'Design system tab styling'
      via: 'bg-accent rounded-md pattern'
      pattern: 'bg-accent.*rounded-md'
---

<objective>
Gap Closure - Sidebar Tabs Pill Style

Purpose: Fix GAP-01 from phase verification - the filter/manage tabs in Sidebar.tsx popover still use underline styling (`border-b-2`) instead of the pill styling (`bg-accent rounded-md`) used by all other tab bars.

Output: Consistent pill-styled tabs throughout the application.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-navigation-shell/03-VERIFICATION.md
@apps/electron/src/renderer/src/components/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update filter/manage tabs to pill style</name>
  <files>apps/electron/src/renderer/src/components/Sidebar.tsx</files>
  <action>
    Update the filter/manage tab buttons in the TagFilterPopover component to use pill styling instead of underline styling.

    At line 1661, replace:
    `'border-primary text-foreground border-b-2'`
    with:
    `'bg-accent text-foreground rounded-md'`

    At line 1672, replace:
    `'border-primary text-foreground border-b-2'`
    with:
    `'bg-accent text-foreground rounded-md'`

    Also remove the `border-b` class from the parent div (line 1656) since we no longer need the underline container styling. The tabs should float freely with pill backgrounds.

  </action>
  <verify>
    1. Run `grep -n "border-b-2" apps/electron/src/renderer/src/components/Sidebar.tsx` - should return no results for the filter/manage tabs area
    2. Run `grep -n "bg-accent.*rounded-md" apps/electron/src/renderer/src/components/Sidebar.tsx` - should show the updated tab styling
    3. Visual check: Open app, click tag filter button in sidebar, verify filter/manage tabs show pill highlight on active state
  </verify>
  <done>
    Filter and manage tabs in the Sidebar popover use `bg-accent text-foreground rounded-md` styling, matching ConnectionTabBar, DataTabBar, and QueryTabBar patterns.
  </done>
</task>

</tasks>

<verification>
1. No `border-b-2` underline styling remains on filter/manage tabs
2. Active tab shows pill background (`bg-accent`) with rounded corners
3. Visual consistency with other tab bars in the application
</verification>

<success_criteria>

- GAP-01 resolved: Filter/manage tabs use pill styling
- Phase 3 verification score improves to 4/4 must-haves
  </success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-shell/03-04-SUMMARY.md`
</output>
