---
phase: 05-interaction-system
plan: 03
type: execute
wave: 2
depends_on: ['05-01']
files_modified:
  - apps/electron/src/renderer/src/components/Toolbar.tsx
  - apps/electron/src/renderer/src/components/Sidebar.tsx
  - apps/electron/src/renderer/src/components/ConnectionSelector.tsx
  - apps/electron/src/renderer/src/components/query-editor/QueryTabBar.tsx
  - apps/electron/src/renderer/src/components/er-diagram/ERControls.tsx
  - apps/electron/src/renderer/src/components/Titlebar.tsx
autonomous: true

must_haves:
  truths:
    - 'Menu items display their keyboard shortcuts inline (right-aligned)'
    - 'Shortcuts use ShortcutKbd component for consistent formatting'
    - 'Users can discover shortcuts by opening context menus'
  artifacts:
    - path: 'apps/electron/src/renderer/src/components/Toolbar.tsx'
      provides: 'Toolbar menus with inline shortcuts'
      contains: 'ShortcutKbd'
    - path: 'apps/electron/src/renderer/src/components/Sidebar.tsx'
      provides: 'Sidebar context menus with inline shortcuts'
      contains: 'DropdownMenuShortcut'
  key_links:
    - from: 'Toolbar.tsx, Sidebar.tsx'
      to: 'keyboard-shortcuts-store.ts'
      via: 'ShortcutKbd component or DropdownMenuShortcut'
      pattern: 'ShortcutKbd|DropdownMenuShortcut'
---

<objective>
Add inline keyboard shortcuts to dropdown menus throughout the app.

Purpose: Enable users to discover keyboard shortcuts by viewing them inline within menu items, improving discoverability and keyboard-first workflow.

Output: All major dropdown menus display their associated keyboard shortcuts inline.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-interaction-system/05-RESEARCH.md
@.planning/phases/05-interaction-system/05-01-SUMMARY.md (for transition patterns)

Key reference:

- ShortcutKbd component exists in @apps/electron/src/renderer/src/components/ShortcutKbd.tsx
- DropdownMenuShortcut exists in @packages/ui/src/dropdown-menu.tsx
- Keyboard shortcuts store at @apps/electron/src/renderer/src/stores/keyboard-shortcuts-store.ts
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline shortcuts to Toolbar and Titlebar menus</name>
  <files>
    apps/electron/src/renderer/src/components/Toolbar.tsx
    apps/electron/src/renderer/src/components/Titlebar.tsx
  </files>
  <action>
Add inline keyboard shortcuts to dropdown menu items in Toolbar and Titlebar.

**Pattern to follow:**

```tsx
import { DropdownMenuShortcut } from '@sqlpro/ui/dropdown-menu';
// OR use ShortcutKbd for store-based shortcuts:
import { ShortcutKbd } from '@/components/ShortcutKbd';

// In menu items:
<DropdownMenuItem onSelect={...}>
  <Icon className="mr-2 h-4 w-4" />
  Action Label
  <DropdownMenuShortcut>⌘N</DropdownMenuShortcut>
</DropdownMenuItem>

// OR for store-based shortcuts:
<DropdownMenuItem onSelect={...}>
  <Icon className="mr-2 h-4 w-4" />
  Action Label
  <ShortcutKbd action="action.id" className="ml-auto" />
</DropdownMenuItem>
```

**Toolbar.tsx:**
Find all DropdownMenuItem components and add shortcuts for actions that have them:

- New Query: ⌘N
- Run Query: ⌘⏎ (or ⌘Enter)
- Format Query: ⌘⇧F
- Save Query: ⌘S
- Any other actions with shortcuts in the keyboard-shortcuts-store

**Titlebar.tsx:**
Find all DropdownMenuItem components and add shortcuts:

- Settings: ⌘,
- Toggle Theme: (if has shortcut)
- Any other actions with shortcuts

Use DropdownMenuShortcut for hardcoded shortcuts or ShortcutKbd for dynamic store-based shortcuts. Prefer ShortcutKbd if the action exists in the keyboard shortcuts store.
</action>
<verify>
Grep for "DropdownMenuShortcut\|ShortcutKbd" in Toolbar.tsx and Titlebar.tsx.
</verify>
<done>
Toolbar menu items display inline keyboard shortcuts.
Titlebar menu items display inline keyboard shortcuts.
</done>
</task>

<task type="auto">
  <name>Task 2: Add inline shortcuts to remaining menus</name>
  <files>
    apps/electron/src/renderer/src/components/Sidebar.tsx
    apps/electron/src/renderer/src/components/ConnectionSelector.tsx
    apps/electron/src/renderer/src/components/query-editor/QueryTabBar.tsx
    apps/electron/src/renderer/src/components/er-diagram/ERControls.tsx
  </files>
  <action>
Add inline keyboard shortcuts to dropdown menu items in remaining components.

**Sidebar.tsx:**
Find context menus and add shortcuts where applicable:

- Refresh Schema: ⌘R (if exists)
- Any table/schema actions with shortcuts

**ConnectionSelector.tsx:**
Find dropdown menus and add shortcuts:

- New Connection: (if has shortcut)
- Disconnect: (if has shortcut)

**QueryTabBar.tsx:**
Find tab context menus and add shortcuts:

- Close Tab: ⌘W
- Close Other Tabs: ⌘⌥W (if exists)
- Rename Tab: (if exists)

**ERControls.tsx:**
Find dropdown menus and add shortcuts:

- Zoom In: ⌘+
- Zoom Out: ⌘-
- Fit to Screen: ⌘0 (if exists)

For each file:

1. Check what menu items exist
2. Cross-reference with keyboard-shortcuts-store to find matching shortcuts
3. Add DropdownMenuShortcut or ShortcutKbd to menu items that have shortcuts
4. Skip items that don't have associated shortcuts

Note: Not all menu items will have shortcuts. Only add shortcuts for items that actually have them defined in the keyboard shortcuts system.
</action>
<verify>
Grep for "DropdownMenuShortcut\|ShortcutKbd" in Sidebar.tsx, ConnectionSelector.tsx, QueryTabBar.tsx, ERControls.tsx.
At least some of these files should have inline shortcuts.
</verify>
<done>
Sidebar context menus display inline shortcuts where applicable.
Connection selector menus display inline shortcuts where applicable.
Query tab menus display inline shortcuts where applicable.
ER diagram control menus display inline shortcuts where applicable.
</done>
</task>

</tasks>

<verification>
1. Open app with `pnpm dev:electron`
2. Click on Toolbar menu buttons - shortcuts should appear right-aligned in menu items
3. Right-click on sidebar items - context menus should show shortcuts where applicable
4. Right-click on query tabs - tab menu should show Close Tab shortcut
5. Check ER diagram controls dropdown - zoom shortcuts should be visible
6. Verify shortcuts match actual keyboard shortcuts (try pressing them)
</verification>

<success_criteria>

- All major dropdown menus display inline keyboard shortcuts
- Shortcuts are right-aligned and use consistent formatting
- Users can discover shortcuts by opening menus
- Displayed shortcuts match actual keyboard shortcuts
  </success_criteria>

<output>
After completion, create `.planning/phases/05-interaction-system/05-03-SUMMARY.md`
</output>
