---
phase: 05-interaction-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/electron/src/renderer/src/styles/globals.css
  - packages/ui/src/button.tsx
  - packages/ui/src/dropdown-menu.tsx
  - packages/ui/src/command.tsx
autonomous: true

must_haves:
  truths:
    - 'Focus rings only appear when navigating with keyboard (Tab), not on mouse clicks'
    - 'Transitions across interactive elements use 100-200ms timing with ease-out'
    - 'Button hover/active transitions feel smooth and responsive'
    - 'Dropdown menu items have subtle transition on focus/hover'
  artifacts:
    - path: 'apps/electron/src/renderer/src/styles/globals.css'
      provides: 'Transition utility classes using design tokens'
      contains: '.transition-fast'
    - path: 'packages/ui/src/button.tsx'
      provides: 'Standardized button transitions'
      contains: 'duration-100'
    - path: 'packages/ui/src/dropdown-menu.tsx'
      provides: 'Menu item transitions'
      contains: 'transition-colors'
    - path: 'packages/ui/src/command.tsx'
      provides: 'Command item transitions'
      contains: 'transition-colors'
  key_links:
    - from: 'globals.css'
      to: 'button.tsx, dropdown-menu.tsx, command.tsx'
      via: 'CSS custom properties for duration/easing'
      pattern: '--duration-fast|--ease-out'
---

<objective>
Standardize transitions and ensure focus rings only appear during keyboard navigation.

Purpose: Create a consistent, polished interaction feel across all interactive elements by standardizing transition timing to 100-200ms with ease-out, and ensuring focus rings follow the keyboard-only pattern established in globals.css.

Output: Updated CSS utility classes and component-level transitions that create smooth, Linear-style micro-interactions.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-interaction-system/05-RESEARCH.md

Key reference files:
@apps/electron/src/renderer/src/styles/globals.css (lines 324-334 for existing duration tokens)
@packages/ui/src/button.tsx
@packages/ui/src/dropdown-menu.tsx
@packages/ui/src/command.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transition utility classes to globals.css</name>
  <files>apps/electron/src/renderer/src/styles/globals.css</files>
  <action>
Add transition utility classes that use the existing design tokens (--duration-fast, --duration-normal, --duration-slow, --ease-out).

Add after the existing spacing/shadow variables section, within @layer base or as utility classes:

```css
/* Transition utilities using design tokens */
.transition-fast {
  transition: all var(--duration-fast) var(--ease-out);
}
.transition-normal {
  transition: all var(--duration-normal) var(--ease-out);
}
.transition-slow {
  transition: all var(--duration-slow) var(--ease-out);
}
/* Color-only transitions (lighter, for backgrounds) */
.transition-colors-fast {
  transition:
    color var(--duration-fast) var(--ease-out),
    background-color var(--duration-fast) var(--ease-out),
    border-color var(--duration-fast) var(--ease-out);
}
```

Also verify the existing focus-visible rules (lines 427-436) are correct - they should already hide focus on mouse click.
</action>
<verify>
Grep for `.transition-fast` in globals.css to confirm classes exist.
Grep for `:focus:not(:focus-visible)` to confirm focus rules are present.
</verify>
<done>
Transition utility classes exist using design tokens.
Focus ring rules hide outline on mouse clicks but show on keyboard navigation.
</done>
</task>

<task type="auto">
  <name>Task 2: Standardize transitions in button, dropdown-menu, and command components</name>
  <files>
    packages/ui/src/button.tsx
    packages/ui/src/dropdown-menu.tsx
    packages/ui/src/command.tsx
  </files>
  <action>
Update the Tailwind classes in each component to use explicit duration and easing:

**button.tsx:**

- In buttonVariants base class, replace `transition-all` with `transition-all duration-100 ease-out`
- This ensures button hover/active states have consistent 100ms timing

**dropdown-menu.tsx:**

- In DropdownMenuItem className, add `transition-colors duration-100 ease-out` for smooth focus/hover
- In DropdownMenuSubTrigger className, add `transition-colors duration-100 ease-out`
- In DropdownMenuCheckboxItem className, add `transition-colors duration-100 ease-out`
- In DropdownMenuRadioItem className, add `transition-colors duration-100 ease-out`

**command.tsx:**

- In CommandItem className, add `transition-colors duration-100 ease-out` for selected/hover states
- This creates smooth state changes when navigating with arrows

Note: Use Tailwind's built-in `duration-100` and `ease-out` classes rather than CSS custom properties for consistency with existing component patterns.
</action>
<verify>
Run: `grep -n "duration-100" packages/ui/src/button.tsx packages/ui/src/dropdown-menu.tsx packages/ui/src/command.tsx`
All three files should show the duration-100 class.
</verify>
<done>
Button component has explicit 100ms transitions.
DropdownMenuItem and variants have transition-colors duration-100.
CommandItem has transition-colors duration-100 for smooth selection changes.
</done>
</task>

</tasks>

<verification>
1. Focus ring test:
   - Start app with `pnpm dev:electron`
   - Click a button - no focus ring should appear
   - Press Tab to focus the same button - focus ring should appear

2. Transition test:
   - Hover over buttons - transition should feel smooth (100ms)
   - Open command palette (Cmd+K), arrow through items - selection changes should be smooth
   - Open a dropdown menu, hover over items - background color change should be smooth
     </verification>

<success_criteria>

- Focus rings only appear on keyboard navigation (Tab key), not on mouse clicks
- All interactive elements use 100-200ms transitions with ease-out timing
- Transitions feel smooth and responsive, not instant or sluggish
- No visual regressions in existing components
  </success_criteria>

<output>
After completion, create `.planning/phases/05-interaction-system/05-01-SUMMARY.md`
</output>
