---
phase: 10-website-content
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/src/components/Download.tsx
  - apps/website/src/components/Download.css
  - apps/website/src/components/Footer.tsx
  - apps/website/src/components/Footer.css
  - apps/website/src/locales/en.json
  - apps/website/src/locales/zh.json
autonomous: true

must_haves:
  truths:
    - 'Download section has single prominent CTA'
    - 'Trust badges and build guide are removed'
    - 'Footer contains only essential links'
    - 'Footer CTA banner is removed'
    - "No placeholder '#' links remain"
  artifacts:
    - path: 'apps/website/src/components/Download.tsx'
      provides: 'Streamlined download section'
      pattern: '(?!.*trust)'
    - path: 'apps/website/src/components/Footer.tsx'
      provides: 'Minimal footer with essential links'
      pattern: '(?!.*footer-cta)'
  key_links:
    - from: 'apps/website/src/components/Footer.tsx'
      to: 'https://github.com'
      via: 'href attribute'
      pattern: 'href=.*github'
---

<objective>
Streamline the Download section and minimize the Footer to essential links only.

Purpose: Remove marketing clutter from the page bottom. Download becomes single-action focused. Footer becomes minimal with only real, working links (GitHub, releases, documentation).

Output:

- Download.tsx with single CTA focus (remove trust badges, build guide)
- Download.css cleaned of removed elements
- Footer.tsx with minimal links (no 4-column grid, no CTA banner)
- Footer.css simplified
- Updated translation keys for both sections
  </objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-website-content/10-RESEARCH.md

# Source files

@apps/website/src/components/Download.tsx
@apps/website/src/components/Download.css
@apps/website/src/components/Footer.tsx
@apps/website/src/components/Footer.css
@apps/website/src/locales/en.json
@apps/website/src/locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Streamline Download section</name>
  <files>
    apps/website/src/components/Download.tsx
    apps/website/src/components/Download.css
  </files>
  <action>
1. **Simplify Download.tsx structure**:

Keep:

- Section title (can simplify to just "Download SQL Pro")
- Platform detection logic (detectPlatform function)
- Primary download button with platform-specific text
- "Other platforms" links row

Remove:

- Label badge at top (if present)
- Subtitle text (redundant with single CTA)
- Trust badges section (secure payment, guarantee, Stripe - not needed for open source)
- Build from source note and link (put in documentation instead)

Target structure:

```tsx
<section id="download" className="download">
  <div className="download-container">
    <h2 className="download-title">Download SQL Pro</h2>

    {/* Primary download button - platform detected */}
    <a href={getDownloadUrl()} className="download-btn-primary">
      <DownloadIcon />
      {t('download.downloadFor', { platform: platformName })}
    </a>

    {/* Architecture info */}
    <span className="download-arch">{archInfo}</span>

    {/* Other platforms row */}
    <div className="download-platforms">
      <span>{t('download.otherPlatforms')}</span>
      {otherPlatforms.map(...)}
    </div>
  </div>
</section>
```

2. **Update Download.css**:

- Remove trust badge styles (`.download-trust`, `.trust-item`, etc.)
- Remove build guide styles if any
- Keep download button and platform link styles
- Ensure centered layout with proper spacing
- Keep responsive styles for platform buttons

3. **Keep platform detection logic** - this is valuable functionality:

- detectPlatform() function
- getDownloadUrl() function
- Platform-specific button text
  </action>
  <verify>
  Run `pnpm dev:website` and verify:
- Download section shows single prominent button
- Button text shows detected platform (e.g., "Download for macOS")
- Architecture info displayed (e.g., "Universal M1/M2/M3/M4 + Intel")
- Other platforms shown as smaller links below
- No trust badges visible
- No "build from source" text visible
- Clean, focused appearance
  </verify>
  <done>
  Download section is streamlined to single CTA with platform detection. Trust badges and build guide removed.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Minimize Footer to essential links</name>
  <files>
    apps/website/src/components/Footer.tsx
    apps/website/src/components/Footer.css
  </files>
  <action>
1. **Dramatically simplify Footer.tsx**:

Remove entirely:

- CTA banner section ("Ready to transform your database workflow?")
- 4-column link grid (Product, Resources, Company, Support columns)
- Placeholder links (href="#") - these go nowhere
- Twitter and Discord social links (keep only GitHub)
- Legal links (Privacy Policy, Terms, Cookies) - all are "#" placeholders

Keep:

- Brand logo/name
- Short tagline (one line)
- Essential links as inline list:
  - GitHub (real link)
  - Documentation (real link if exists, or remove)
  - Releases/Changelog (real link)
  - Discussions/Community (real link if exists)
- Copyright

Target structure:

```tsx
const essentialLinks = [
  {
    label: 'GitHub',
    href: 'https://github.com/kunish-homelab/sql-pro',
    external: true,
  },
  {
    label: 'Releases',
    href: 'https://github.com/kunish-homelab/sql-pro/releases',
    external: true,
  },
  {
    label: 'Documentation',
    href: 'https://kunish-homelab.github.io/sql-pro/',
    external: true,
  },
  {
    label: 'Discussions',
    href: 'https://github.com/kunish-homelab/sql-pro/discussions',
    external: true,
  },
];

<footer className="footer">
  <div className="footer-container">
    <div className="footer-content">
      {/* Brand */}
      <a href="#" className="footer-logo">
        <img src="/icon.svg" alt="" width="32" height="32" />
        SQL Pro
      </a>

      {/* Essential links - inline */}
      <nav className="footer-links">
        {essentialLinks.map((link) => (
          <a
            key={link.label}
            href={link.href}
            target="_blank"
            rel="noopener noreferrer"
          >
            {link.label}
          </a>
        ))}
      </nav>
    </div>

    {/* Copyright */}
    <p className="footer-copyright">
      {t('footer.copyright', { year: currentYear })}
    </p>
  </div>
</footer>;
```

2. **Simplify Footer.css**:

Remove:

- `.footer-cta` styles (entire CTA banner)
- `.footer-grid` with 5 columns
- `.footer-column` styles
- `.footer-column-title` styles
- `.footer-legal` styles

Add/keep:

- Simple centered footer layout
- Inline link list style
- Logo + links in horizontal layout on desktop
- Stack vertically on mobile
- Subtle styling (no heavy backgrounds)

```css
.footer {
  padding: 48px 24px;
  border-top: 1px solid var(--border);
}

.footer-container {
  max-width: 1200px;
  margin: 0 auto;
  text-align: center;
}

.footer-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 24px;
  margin-bottom: 24px;
}

.footer-logo {
  display: flex;
  align-items: center;
  gap: 8px;
  font-weight: 600;
  color: var(--foreground);
  text-decoration: none;
}

.footer-links {
  display: flex;
  gap: 24px;
}

.footer-links a {
  color: var(--muted-foreground);
  text-decoration: none;
  transition: color 0.2s;
}

.footer-links a:hover {
  color: var(--foreground);
}

.footer-copyright {
  color: var(--muted-foreground);
  font-size: 14px;
}

@media (max-width: 640px) {
  .footer-content {
    flex-direction: column;
    text-align: center;
  }
  .footer-links {
    flex-wrap: wrap;
    justify-content: center;
  }
}
```

  </action>
  <verify>
Run `pnpm dev:website` and verify:
- No CTA banner in footer
- No 4-column link grid
- Only essential links visible (GitHub, Releases, Docs, Discussions)
- All visible links are real URLs (no "#" placeholders)
- Logo and copyright present
- Clean minimal appearance
- Footer is much shorter than before
- Responsive on mobile (links wrap/stack)
  </verify>
  <done>
Footer is minimized to essential links only. CTA banner and placeholder links removed. Clean, minimal design.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update translation keys for Download and Footer</name>
  <files>
    apps/website/src/locales/en.json
    apps/website/src/locales/zh.json
  </files>
  <action>
1. **Update en.json download section**:
```json
"download": {
  "title": "Download",
  "titleHighlight": "SQL Pro",
  "downloadFor": "Download for {{platform}}",
  "otherPlatforms": "Other platforms:",
  "platforms": {
    "mac": "macOS",
    "windows": "Windows",
    "linux": "Linux",
    "unknown": "Download"
  },
  "arch": {
    "mac": "Universal (M1/M2/M3/M4 + Intel)",
    "windows": "x64",
    "linux": "x64 (AppImage / deb)",
    "unknown": "All platforms"
  }
}
```
Remove: `subtitle`, `buildGuide`, `buildGuideLink` (no longer used)

2. **Update en.json footer section**:

```json
"footer": {
  "github": "GitHub",
  "releases": "Releases",
  "docs": "Documentation",
  "discussions": "Discussions",
  "copyright": "© {{year}} SQL Pro. Open source under MIT license."
}
```

Remove: `techStack`, `issues` (consolidate to GitHub link)

3. **Update zh.json download section**:

```json
"download": {
  "title": "下载",
  "titleHighlight": "SQL Pro",
  "downloadFor": "下载 {{platform}} 版本",
  "otherPlatforms": "其他平台：",
  "platforms": {
    "mac": "macOS",
    "windows": "Windows",
    "linux": "Linux",
    "unknown": "下载"
  },
  "arch": {
    "mac": "Universal (M1/M2/M3/M4 + Intel)",
    "windows": "x64",
    "linux": "x64 (AppImage / deb)",
    "unknown": "所有平台"
  }
}
```

4. **Update zh.json footer section**:

```json
"footer": {
  "github": "GitHub",
  "releases": "发布记录",
  "docs": "文档",
  "discussions": "讨论区",
  "copyright": "© {{year}} SQL Pro. 基于 MIT 协议开源。"
}
```

Keep all translation keys that are still referenced. Only remove keys for deleted UI elements.
</action>
<verify>
Run `pnpm dev:website` and verify:

- No console errors about missing translation keys
- Download section text displays correctly
- Footer links text displays correctly
- Switch to Chinese - all text renders correctly
- No visible translation key strings (like "download.subtitle")
  </verify>
  <done>
  Translation files updated for streamlined Download and Footer. Removed unused keys, kept all referenced ones.
  </done>
  </task>

</tasks>

<verification>
After completing all tasks:
1. `pnpm dev:website` runs without errors
2. Download section: single CTA button, platform detection works, no trust badges
3. Footer: minimal links (4-5 links), no CTA banner, no 4-column grid
4. All footer links are real URLs (click and verify they work)
5. No placeholder "#" links visible anywhere
6. Both English and Chinese work correctly
7. Visual inspection: page bottom is clean and minimal
8. Responsive layouts work on mobile
</verification>

<success_criteria>

- Download section has single prominent CTA with platform detection
- Trust badges and build guide text removed from Download
- Footer CTA banner removed
- Footer link columns (Product, Resources, Company, Support) removed
- Footer has only essential links: GitHub, Releases, Docs, Discussions
- All visible links have real destinations (no "#" placeholders)
- Both en.json and zh.json updated appropriately
- No console errors or missing translations
  </success_criteria>

<output>
After completion, create `.planning/phases/10-website-content/10-03-SUMMARY.md`
</output>
