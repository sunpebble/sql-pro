---
phase: 10-website-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/src/components/Hero.tsx
  - apps/website/src/components/Hero.css
  - apps/website/src/App.tsx
  - apps/website/src/locales/en.json
  - apps/website/src/locales/zh.json
autonomous: true

must_haves:
  truths:
    - 'Hero section displays actual product screenshot prominently'
    - 'Marketing copy is concise (tagline, not paragraph)'
    - 'Pricing section is removed from page'
    - 'Simulated preview card is replaced with real screenshot'
  artifacts:
    - path: 'apps/website/src/components/Hero.tsx'
      provides: 'Screenshot-first hero with minimal copy'
      contains: '<img'
    - path: 'apps/website/src/App.tsx'
      provides: 'Page structure without Pricing'
      pattern: '(?!.*Pricing)'
  key_links:
    - from: 'apps/website/src/components/Hero.tsx'
      to: '/screenshots/query-dark.png'
      via: 'img src attribute'
      pattern: "src=.*screenshots.*\\.png"
---

<objective>
Simplify the Hero section to product-focused design and remove Pricing from App.tsx.

Purpose: Transform the hero from marketing-heavy layout to Linear/Raycast-style screenshot-first design. The product screenshot becomes the visual anchor, with minimal supporting copy.

Output:

- Hero.tsx with actual product screenshot instead of simulated preview card
- Simplified hero copy (tagline instead of paragraph)
- Hero.css cleaned of preview card styles
- App.tsx without Pricing component
- Updated translation keys for hero section
  </objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-website-content/10-RESEARCH.md

# Source files

@apps/website/src/components/Hero.tsx
@apps/website/src/components/Hero.css
@apps/website/src/App.tsx
@apps/website/src/locales/en.json
@apps/website/src/locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify Hero.tsx to screenshot-first design</name>
  <files>
    apps/website/src/components/Hero.tsx
    apps/website/src/components/Hero.css
  </files>
  <action>
Transform Hero.tsx from marketing-heavy to product-focused:

1. **Remove the simulated preview card** (lines 73-178):
   - Delete the entire `.hero-preview` div with its fake app UI
   - This is a DOM element that simulates the app, not a real screenshot

2. **Replace with actual product screenshot**:
   - Add an `<img>` element pointing to `/screenshots/query-dark.png` (best showcases the app)
   - Add appropriate alt text for accessibility
   - Wrap in a container with shadow/rounded corners for visual polish

3. **Simplify hero header content**:
   - Keep the "Open Source" badge and version (lines 12-26)
   - Simplify the h1 title - make it single line: "SQL Pro"
   - Keep subtitle from translation but make it the tagline
   - The description should be very short (use translation key)

4. **Remove hero-tags section** (lines 180-221):
   - These are redundant with the Features section
   - Delete the entire `.hero-tags` div

5. **Keep the CTA buttons** (Download, GitHub) - they work well

6. **Update Hero.css**:
   - Remove all `.hero-preview-*` styles (the simulated preview card)
   - Remove `.hero-tags` styles
   - Add `.hero-screenshot` container style:
     - max-width: 100%
     - border-radius: var(--border-radius-xl)
     - box-shadow for subtle depth
     - margin-top spacing
   - Ensure responsive image sizing

Structure after changes:

```tsx
<section className="hero">
  <div className="hero-container">
    {/* Badge + version */}
    {/* Single-line title + tagline */}
    {/* Two CTA buttons */}
    {/* Screenshot image */}
  </div>
</section>
```

  </action>
  <verify>
Run `pnpm dev:website` and verify:
- Hero displays actual product screenshot (not simulated UI)
- Screenshot shows query editor interface
- Title is concise, not multi-line
- No feature tags below the screenshot
- CTA buttons (Download, GitHub) are present and styled correctly
  </verify>
  <done>
Hero section shows actual product screenshot prominently with minimal marketing copy. Simulated preview card and feature tags are removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Pricing section and update translations</name>
  <files>
    apps/website/src/App.tsx
    apps/website/src/locales/en.json
    apps/website/src/locales/zh.json
  </files>
  <action>
1. **Update App.tsx**:
   - Remove the Pricing import: `import Pricing from './components/Pricing';`
   - Remove the `<Pricing />` component from the main content (currently between Features and Download)
   - Keep all other imports and components unchanged

2. **Update en.json hero section** for concise copy:
   - Change `hero.description` to a short tagline: "The database manager developers love."
   - Keep other hero keys as they are (title, subtitle, download, github work fine)

3. **Update zh.json hero section** to match:
   - Change `hero.description` to Chinese equivalent: "开发者喜爱的数据库管理工具。"
   - Keep other hero keys unchanged

4. **Keep pricing translation keys** (do not remove):
   - The Pricing component is still in the codebase (just not rendered)
   - Removing translation keys could cause build errors if component is re-added later
   - This is safe removal - just not importing/rendering
     </action>
     <verify>
     Run `pnpm dev:website` and verify:

- Page loads without Pricing section
- Order is: Hero -> Features -> Download -> Footer
- No console errors about missing translations
- Switch language to Chinese and verify hero description shows correctly
  </verify>
  <done>
  Pricing section removed from page. Hero description simplified to concise tagline in both languages.
  </done>
  </task>

</tasks>

<verification>
After completing both tasks:
1. `pnpm dev:website` runs without errors
2. Hero shows actual screenshot (query-dark.png visible)
3. Hero title/description is concise, not multi-line marketing copy
4. No simulated preview card (fake window chrome gone)
5. No feature tags below hero
6. No Pricing section on page
7. Language switch works correctly
8. Visual inspection confirms Linear/Raycast-style product focus
</verification>

<success_criteria>

- Hero section displays actual product screenshot as visual anchor
- Marketing copy is reduced to single tagline
- Simulated preview card is completely removed
- Feature tags are removed from hero
- Pricing section is not rendered on page
- Both en.json and zh.json hero sections are updated
- Page loads without console errors
  </success_criteria>

<output>
After completion, create `.planning/phases/10-website-content/10-01-SUMMARY.md`
</output>
