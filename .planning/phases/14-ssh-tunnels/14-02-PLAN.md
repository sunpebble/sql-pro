---
phase: 14-ssh-tunnels
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/electron/src/renderer/src/components/SSHTunnelConfig.tsx
  - apps/electron/src/renderer/src/components/ServerConnectionDialog.tsx
  - apps/electron/src/renderer/src/locales/en/dialog.json
  - apps/electron/src/renderer/src/locales/zh/dialog.json
autonomous: true

must_haves:
  truths:
    - 'User can see SSH tunnel configuration section in connection dialog'
    - 'User can toggle SSH tunnel on/off for MySQL/PostgreSQL connections'
    - 'User can enter SSH host, port, username, and choose auth method'
    - 'User can enter password or select private key file'
    - 'User can optionally configure jump host settings'
  artifacts:
    - path: 'apps/electron/src/renderer/src/components/SSHTunnelConfig.tsx'
      provides: 'Collapsible SSH tunnel configuration component'
      exports: ['SSHTunnelConfig']
    - path: 'apps/electron/src/renderer/src/components/ServerConnectionDialog.tsx'
      provides: 'Updated dialog with SSH tunnel section'
      contains: 'SSHTunnelConfig'
  key_links:
    - from: 'apps/electron/src/renderer/src/components/ServerConnectionDialog.tsx'
      to: 'apps/electron/src/renderer/src/components/SSHTunnelConfig.tsx'
      via: 'Component import and render'
      pattern: '<SSHTunnelConfig'
---

<objective>
Create the SSH tunnel configuration UI component and integrate it into the ServerConnectionDialog.

Purpose: Users can configure SSH tunnel settings (host, port, auth method, credentials, jump host) when setting up MySQL/PostgreSQL connections.

Output: SSHTunnelConfig component integrated into ServerConnectionDialog with full i18n support.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ssh-tunnels/14-RESEARCH.md

# Reference UI patterns

@apps/electron/src/renderer/src/components/ServerConnectionDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SSHTunnelConfig component</name>
  <files>
    apps/electron/src/renderer/src/components/SSHTunnelConfig.tsx
  </files>
  <action>
Create `SSHTunnelConfig.tsx` - a collapsible section for SSH tunnel settings:

1. Define props interface `SSHTunnelConfigProps`:
   - enabled: boolean
   - onEnabledChange: (enabled: boolean) => void
   - sshHost: string, onSshHostChange: (host: string) => void
   - sshPort: string, onSshPortChange: (port: string) => void
   - sshUsername: string, onSshUsernameChange: (username: string) => void
   - sshAuthMethod: 'password' | 'privateKey'
   - onSshAuthMethodChange: (method: 'password' | 'privateKey') => void
   - sshPassword: string, onSshPasswordChange: (password: string) => void
   - sshPrivateKeyPath: string, onSshPrivateKeyPathChange: (path: string) => void
   - sshPassphrase: string, onSshPassphraseChange: (passphrase: string) => void
   - Jump host props (same pattern with jumpHost prefix)
   - showJumpHost: boolean, onShowJumpHostChange: (show: boolean) => void
   - disabled?: boolean (for when connecting)

2. UI Structure (use shadcn/ui components):
   - Collapsible section with Checkbox toggle "Use SSH Tunnel"
   - When enabled, show fields:
     - SSH Host (required): Input, placeholder "ssh.example.com"
     - SSH Port: Input type="number", placeholder "22"
     - SSH Username (required): Input, placeholder "ubuntu"
     - Auth Method: RadioGroup with "Password" and "Private Key" options
     - If password: Password input field
     - If privateKey:
       - File path input with "Browse" button (uses window.sqlPro.dialog.openFile)
       - Passphrase input (optional, for encrypted keys)
     - Checkbox: "Use Jump Host (Bastion)"
     - If jump host enabled: repeat SSH fields with "Jump Host" labels

3. Styling:
   - Use existing dialog styling patterns
   - Group related fields visually
   - Subtle border/background for SSH section (border-l-2 border-primary/20 pl-4)
   - Use Collapsible from shadcn/ui for expand/collapse animation
   - Use i18n for all labels via useTranslation('dialog')

4. Import from @sqlpro/ui: Button, Checkbox, Input, Label, RadioGroup, RadioGroupItem
   Import Collapsible, CollapsibleContent, CollapsibleTrigger from @/components/ui/collapsible
   </action>
   <verify>
   TypeScript compiles: `cd apps/electron && pnpm tsc --noEmit`
   </verify>
   <done>
   SSHTunnelConfig component created with all SSH configuration fields, auth method toggle, and optional jump host section
   </done>
   </task>

<task type="auto">
  <name>Task 2: Integrate SSHTunnelConfig into ServerConnectionDialog</name>
  <files>
    apps/electron/src/renderer/src/components/ServerConnectionDialog.tsx
  </files>
  <action>
Modify ServerConnectionDialog.tsx to include SSH tunnel configuration:

1. Add state for SSH tunnel fields (only for mysql/postgresql):

   ```typescript
   // SSH Tunnel state
   const [sshEnabled, setSshEnabled] = useState(false);
   const [sshHost, setSshHost] = useState('');
   const [sshPort, setSshPort] = useState('22');
   const [sshUsername, setSshUsername] = useState('');
   const [sshAuthMethod, setSshAuthMethod] = useState<
     'password' | 'privateKey'
   >('password');
   const [sshPassword, setSshPassword] = useState('');
   const [sshPrivateKeyPath, setSshPrivateKeyPath] = useState('');
   const [sshPassphrase, setSshPassphrase] = useState('');
   // Jump host state (same pattern)
   const [showJumpHost, setShowJumpHost] = useState(false);
   const [jumpHost, setJumpHost] = useState('');
   const [jumpPort, setJumpPort] = useState('22');
   const [jumpUsername, setJumpUsername] = useState('');
   const [jumpAuthMethod, setJumpAuthMethod] = useState<
     'password' | 'privateKey'
   >('password');
   const [jumpPassword, setJumpPassword] = useState('');
   const [jumpPrivateKeyPath, setJumpPrivateKeyPath] = useState('');
   const [jumpPassphrase, setJumpPassphrase] = useState('');
   ```

2. Reset SSH state in useEffect when dialog opens/closes or mode changes

3. In the form, AFTER the standard connection fields but BEFORE Read-only checkbox:
   - Only show for MySQL and PostgreSQL (not Supabase, Qdrant, Turso, SQLite)
   - Render `<SSHTunnelConfig ... />` with all state props

4. In handleSubmit, if sshEnabled:
   - Add `ssh` field to config with SSHConfig structure
   - Add `sshJumpHost` field if showJumpHost enabled
   - Note: Credentials will be sent to main process and stored separately

5. In edit mode (initialConfig), load SSH settings if present:
   - Set sshEnabled = true if initialConfig.ssh?.enabled
   - Populate all SSH fields from initialConfig.ssh
   - Populate jump host fields from initialConfig.sshJumpHost

6. Update form validation: if SSH enabled, require sshHost and sshUsername
   </action>
   <verify> - TypeScript compiles: `cd apps/electron && pnpm tsc --noEmit` - App starts: `cd apps/electron && pnpm dev` (visual verification available)
   </verify>
   <done>
   ServerConnectionDialog includes SSH tunnel configuration section for MySQL/PostgreSQL connections, with proper state management and form submission
   </done>
   </task>

<task type="auto">
  <name>Task 3: Add i18n translations for SSH tunnel UI</name>
  <files>
    apps/electron/src/renderer/src/locales/en/dialog.json
    apps/electron/src/renderer/src/locales/zh/dialog.json
  </files>
  <action>
Add SSH tunnel translations to both locale files:

1. In `en/dialog.json`, add under "connection" key:

   ```json
   "ssh": {
     "title": "SSH Tunnel",
     "enable": "Use SSH Tunnel",
     "host": "SSH Host",
     "hostPlaceholder": "ssh.example.com",
     "port": "SSH Port",
     "username": "SSH Username",
     "usernamePlaceholder": "ubuntu",
     "authMethod": "Authentication Method",
     "password": "Password",
     "privateKey": "Private Key",
     "privateKeyPath": "Private Key File",
     "privateKeyPlaceholder": "~/.ssh/id_rsa",
     "browse": "Browse",
     "passphrase": "Key Passphrase",
     "passphrasePlaceholder": "Leave empty if key is not encrypted",
     "jumpHost": {
       "enable": "Use Jump Host (Bastion)",
       "title": "Jump Host Configuration",
       "host": "Jump Host",
       "hostPlaceholder": "bastion.example.com"
     }
   }
   ```

2. In `zh/dialog.json`, add corresponding Chinese translations:
   ```json
   "ssh": {
     "title": "SSH 隧道",
     "enable": "使用 SSH 隧道",
     "host": "SSH 主机",
     "hostPlaceholder": "ssh.example.com",
     "port": "SSH 端口",
     "username": "SSH 用户名",
     "usernamePlaceholder": "ubuntu",
     "authMethod": "认证方式",
     "password": "密码",
     "privateKey": "私钥",
     "privateKeyPath": "私钥文件",
     "privateKeyPlaceholder": "~/.ssh/id_rsa",
     "browse": "浏览",
     "passphrase": "密钥密码",
     "passphrasePlaceholder": "如私钥未加密请留空",
     "jumpHost": {
       "enable": "使用跳板机 (Bastion)",
       "title": "跳板机配置",
       "host": "跳板机主机",
       "hostPlaceholder": "bastion.example.com"
     }
   }
   ```
     </action>
     <verify>
       JSON files are valid: `cd apps/electron && node -e "require('./src/renderer/src/locales/en/dialog.json'); require('./src/renderer/src/locales/zh/dialog.json'); console.log('Valid JSON')"`
     </verify>
     <done>
       i18n translations added for all SSH tunnel UI labels in English and Chinese
     </done>
   </task>

</tasks>

<verification>
1. TypeScript compiles: `cd apps/electron && pnpm tsc --noEmit`
2. App starts without errors: `cd apps/electron && pnpm dev`
3. SSH tunnel section visible in MySQL/PostgreSQL connection dialogs
4. All labels translated (switch language to verify)
</verification>

<success_criteria>

- SSHTunnelConfig component renders within ServerConnectionDialog
- SSH section only shows for MySQL and PostgreSQL database types
- User can toggle SSH tunnel, enter host/port/username
- User can choose between password and private key authentication
- User can optionally enable and configure jump host
- All labels use i18n (no hardcoded strings)
  </success_criteria>

<output>
After completion, create `.planning/phases/14-ssh-tunnels/14-02-SUMMARY.md`
</output>
