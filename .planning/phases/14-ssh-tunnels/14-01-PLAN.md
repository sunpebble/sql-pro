---
phase: 14-ssh-tunnels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/electron/package.json
  - apps/electron/src/main/services/ssh/types.ts
  - apps/electron/src/main/services/ssh/ssh-credential-store.ts
  - apps/electron/src/main/services/ssh/tunnel-connection.ts
  - apps/electron/src/main/services/ssh/tunnel-manager.ts
  - apps/electron/src/main/services/ssh/index.ts
  - apps/electron/src/shared/types.ts
autonomous: true

must_haves:
  truths:
    - 'SSH tunnel can be established with password authentication'
    - 'SSH tunnel can be established with private key authentication'
    - 'SSH credentials are encrypted using Electron safeStorage'
    - 'Tunnel dynamically allocates local port to avoid conflicts'
  artifacts:
    - path: 'apps/electron/src/main/services/ssh/tunnel-manager.ts'
      provides: 'TunnelManager class for lifecycle management'
      exports: ['tunnelManager', 'TunnelManager']
    - path: 'apps/electron/src/main/services/ssh/tunnel-connection.ts'
      provides: 'SSHTunnel class for individual tunnel connections'
      exports: ['SSHTunnel']
    - path: 'apps/electron/src/main/services/ssh/ssh-credential-store.ts'
      provides: 'SSH credential encryption/storage service'
      exports: ['sshCredentialStore']
    - path: 'apps/electron/src/main/services/ssh/types.ts'
      provides: 'SSH configuration and status types'
      exports: ['SSHConfig', 'SSHTunnelConfig', 'TunnelStatus']
  key_links:
    - from: 'apps/electron/src/main/services/ssh/ssh-credential-store.ts'
      to: 'electron.safeStorage'
      via: 'safeStorage.encryptString/decryptString'
      pattern: "safeStorage\\.(encrypt|decrypt)String"
    - from: 'apps/electron/src/main/services/ssh/tunnel-connection.ts'
      to: 'ssh2.Client'
      via: 'SSH connection and port forwarding'
      pattern: "new Client\\(\\)"
---

<objective>
Create the core SSH tunnel infrastructure including TunnelManager, SSHTunnel connection class, and SSH credential storage service.

Purpose: Establishes the foundation for SSH tunnel connections with both password and private key authentication, using Electron's safeStorage API for secure credential management.

Output: Complete SSH service layer with types, credential store, tunnel connection class, and tunnel manager.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ssh-tunnels/14-RESEARCH.md

# Reference existing patterns

@apps/electron/src/main/services/password-storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ssh2 and create SSH types</name>
  <files>
    apps/electron/package.json
    apps/electron/src/main/services/ssh/types.ts
    apps/electron/src/shared/types.ts
  </files>
  <action>
1. Install ssh2 and types in apps/electron:
   ```bash
   cd apps/electron && pnpm add ssh2 && pnpm add -D @types/ssh2
   ```

2. Create `apps/electron/src/main/services/ssh/types.ts` with:
   - `SSHAuthMethod`: 'password' | 'privateKey'
   - `SSHConfig` interface:
     - enabled: boolean
     - host: string
     - port?: number (default 22)
     - username: string
     - authMethod: SSHAuthMethod
     - (credentials stored separately via sshCredentialStore)
   - `SSHJumpHostConfig` interface (same structure as SSHConfig for jump host)
   - `SSHTunnelConfig` interface:
     - ssh: SSHConfig
     - jumpHost?: SSHJumpHostConfig
     - remoteHost: string
     - remotePort: number
     - localPort?: number (0 for dynamic)
     - keepaliveInterval?: number (default 10000)
     - readyTimeout?: number (default 20000)
   - `TunnelStatus` interface:
     - state: 'disconnected' | 'connecting' | 'connected' | 'reconnecting' | 'error'
     - localPort?: number
     - error?: string
     - reconnectAttempts?: number

3. Extend `DatabaseConnectionConfig` in `apps/electron/src/shared/types.ts`:
   - Add `ssh?: SSHConfig` field with inline type (for IPC serialization)
   - Add `sshJumpHost?: SSHConfig` field
   - Add JSDoc comments explaining SSH tunnel support
     </action>
     <verify>
   - `pnpm ls ssh2` shows ssh2 installed in apps/electron
   - TypeScript compiles: `cd apps/electron && pnpm tsc --noEmit`
     </verify>
     <done>
     ssh2 library installed, SSH types defined, DatabaseConnectionConfig extended with SSH fields
     </done>
     </task>

<task type="auto">
  <name>Task 2: Create SSH credential store service</name>
  <files>
    apps/electron/src/main/services/ssh/ssh-credential-store.ts
  </files>
  <action>
Create `ssh-credential-store.ts` following the pattern from `password-storage.ts`:

1. Define `SSHCredential` interface:
   - profileId: string (connection profile identifier)
   - password?: string (encrypted)
   - privateKey?: string (encrypted, PEM format)
   - passphrase?: string (encrypted, for encrypted private keys)
   - jumpHostPassword?: string (encrypted)
   - jumpHostPrivateKey?: string (encrypted)
   - jumpHostPassphrase?: string (encrypted)

2. Create `SSHCredentialStore` class with methods:
   - `isAvailable(): boolean` - check if safeStorage encryption is available
   - `saveCredential(profileId: string, credential: Partial<SSHCredential>): boolean` - encrypt and save
   - `getCredential(profileId: string): SSHCredential | null` - decrypt and retrieve
   - `hasCredential(profileId: string): boolean` - check if credentials exist
   - `removeCredential(profileId: string): boolean` - delete credentials
   - `getStoredProfileIds(): string[]` - list all profiles with credentials

3. Implementation details:
   - Use SimpleStore pattern from password-storage.ts
   - Store to `sql-pro-ssh-credentials.json` in app.getPath('userData')
   - Encrypt each sensitive field separately using safeStorage.encryptString()
   - Store encrypted values as base64 strings
   - Handle missing fields gracefully (merge with existing on save)

4. Export singleton: `export const sshCredentialStore = new SSHCredentialStore();`
   </action>
   <verify>
   TypeScript compiles: `cd apps/electron && pnpm tsc --noEmit`
   </verify>
   <done>
   SSHCredentialStore service created with safeStorage encryption for SSH passwords and private keys
   </done>
   </task>

<task type="auto">
  <name>Task 3: Create SSHTunnel connection class and TunnelManager</name>
  <files>
    apps/electron/src/main/services/ssh/tunnel-connection.ts
    apps/electron/src/main/services/ssh/tunnel-manager.ts
    apps/electron/src/main/services/ssh/index.ts
  </files>
  <action>
1. Create `tunnel-connection.ts` with `SSHTunnel` class:
   - Extends EventEmitter for status updates
   - Constructor takes `SSHTunnelConfig` and decrypted credentials
   - Properties:
     - private sshClient: Client | null
     - private jumpClient: Client | null (for jump host)
     - private server: net.Server | null
     - private _localPort: number
     - private _status: TunnelStatus
     - private reconnectAttempts: number
     - private maxReconnectAttempts: number (5)
     - private shouldReconnect: boolean
   - Methods:
     - `async connect(): Promise<number>` - establish tunnel, return local port
     - `private async connectDirect(): Promise<void>` - direct SSH connection
     - `private async connectThroughJumpHost(): Promise<void>` - jump host connection
     - `private buildConnectConfig(config): ConnectConfig` - build ssh2 config
     - `private async startLocalServer(): Promise<number>` - create TCP server for forwarding
     - `private handleDisconnect(): void` - trigger reconnection if enabled
     - `private async reconnect(): Promise<void>` - exponential backoff reconnection
     - `async close(): Promise<void>` - cleanup all resources
     - `get localPort(): number`
     - `get status(): TunnelStatus`
   - Events: 'statusChange', 'connected', 'disconnected', 'reconnected', 'error'

2. Create `tunnel-manager.ts` with `TunnelManager` class:
   - Map of connectionId -> SSHTunnel instances
   - Methods:
     - `async createTunnel(connectionId: string, config: SSHTunnelConfig, credentials: SSHCredential): Promise<number>` - create and connect tunnel
     - `getTunnel(connectionId: string): SSHTunnel | null`
     - `getTunnelStatus(connectionId: string): TunnelStatus | null`
     - `async closeTunnel(connectionId: string): Promise<void>`
     - `closeAllTunnels(): void` - cleanup on app shutdown
     - `hasTunnel(connectionId: string): boolean`
   - Subscribe to tunnel events and forward appropriately

3. Create `index.ts` barrel export:
   - Export tunnelManager singleton
   - Export types from types.ts
   - Export sshCredentialStore
     </action>
     <verify>
     TypeScript compiles: `cd apps/electron && pnpm tsc --noEmit`
     </verify>
     <done>
     SSHTunnel class handles SSH connections with password/key auth, dynamic port forwarding, and auto-reconnection. TunnelManager provides lifecycle management for multiple tunnels.
     </done>
     </task>

</tasks>

<verification>
1. ssh2 package installed: `cd apps/electron && pnpm ls ssh2`
2. All TypeScript compiles: `cd apps/electron && pnpm tsc --noEmit`
3. SSH types exported from services/ssh/index.ts
4. Credential store follows password-storage.ts pattern
</verification>

<success_criteria>

- SSH tunnel infrastructure compiles without errors
- SSHTunnel class supports both password and private key authentication
- TunnelManager can create, track, and close tunnels
- Credentials encrypted via Electron safeStorage API
- Dynamic port allocation using net.createServer with port 0
  </success_criteria>

<output>
After completion, create `.planning/phases/14-ssh-tunnels/14-01-SUMMARY.md`
</output>
