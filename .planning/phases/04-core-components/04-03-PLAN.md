---
phase: 04-core-components
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/checkbox.tsx
  - packages/ui/src/switch.tsx
  - packages/ui/src/radio-group.tsx
autonomous: true

must_haves:
  truths:
    - 'Checkbox has no shadow decoration'
    - 'Switch has no shadow decoration'
    - 'Radio buttons have no shadow decoration'
  artifacts:
    - path: 'packages/ui/src/checkbox.tsx'
      provides: 'Checkbox component without shadow-xs'
      not_contains: 'shadow-xs'
    - path: 'packages/ui/src/switch.tsx'
      provides: 'Switch component without shadow-xs'
      not_contains: 'shadow-xs'
    - path: 'packages/ui/src/radio-group.tsx'
      provides: 'RadioGroup without shadow-xs'
      not_contains: 'shadow-xs'
  key_links:
    - from: 'packages/ui/src/checkbox.tsx'
      to: 'className'
      via: 'cn() merge'
      pattern: 'className.*(?!shadow)'
---

<objective>
Remove decorative shadows from checkbox, switch, and radio-group components to achieve a minimal, flat form control aesthetic.

Purpose: VISL-03 requires refined form styling for dark mode. These toggle/selection controls currently have `shadow-xs` which adds visual weight. The borders and state colors provide sufficient feedback without shadows.

Output: Three form control components with flat styling - no shadows, clean state transitions.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-components/04-RESEARCH.md
@packages/ui/src/checkbox.tsx
@packages/ui/src/switch.tsx
@packages/ui/src/radio-group.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shadow from Checkbox and Switch</name>
  <files>packages/ui/src/checkbox.tsx, packages/ui/src/switch.tsx</files>
  <action>
**checkbox.tsx:**
Remove `shadow-xs` from the CheckboxPrimitive.Root className.

Current className includes (line ~11):

```
...rounded-lg border shadow-xs transition-shadow outline-none...
```

Update to:

```
...rounded-lg border transition-shadow outline-none...
```

Find and remove ` shadow-xs` from the className string.

**switch.tsx:**
Apply the same change - remove `shadow-xs` from wherever it appears in the Switch component's className.

Search for `shadow-xs` and remove it while preserving all other classes.
</action>
<verify>
Run: `grep -n "shadow-xs" packages/ui/src/checkbox.tsx` should return no matches.
Run: `grep -n "shadow-xs" packages/ui/src/switch.tsx` should return no matches.
</verify>
<done>
Checkbox and Switch components no longer have shadow-xs class. They maintain all other styling (borders, checked states, focus rings).
</done>
</task>

<task type="auto">
  <name>Task 2: Remove shadow from RadioGroup</name>
  <files>packages/ui/src/radio-group.tsx</files>
  <action>
Remove `shadow-xs` from the RadioGroupItem component's className.

Search for `shadow-xs` in radio-group.tsx and remove it from the RadioGroupItem styling.

Keep all other styling intact (borders, checked indicator, focus rings).
</action>
<verify>
Run: `grep -n "shadow-xs" packages/ui/src/radio-group.tsx` should return no matches.
Run: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json` should pass.
</verify>
<done>
RadioGroup items no longer have shadow-xs class. Component maintains all functional styling and checked state indicator.
</done>
</task>

</tasks>

<verification>
1. No shadow classes in checkbox.tsx: `grep -c "shadow-xs" packages/ui/src/checkbox.tsx` returns 0
2. No shadow classes in switch.tsx: `grep -c "shadow-xs" packages/ui/src/switch.tsx` returns 0
3. No shadow classes in radio-group.tsx: `grep -c "shadow-xs" packages/ui/src/radio-group.tsx` returns 0
4. TypeScript compiles: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json`
</verification>

<success_criteria>

- Checkbox component has no shadow-xs
- Switch component has no shadow-xs
- RadioGroup items have no shadow-xs
- All components maintain border, focus ring, and checked state styling
- TypeScript compilation passes
  </success_criteria>

<output>
After completion, create `.planning/phases/04-core-components/04-03-SUMMARY.md`
</output>
