---
phase: 04-core-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/button.tsx
  - packages/ui/src/gold-button.tsx
autonomous: true

must_haves:
  truths:
    - 'Button outline variant has no shadow decoration'
    - 'BrandButton default variant has no shadow decorations'
    - 'BrandButton pulse variant removed or flattened'
  artifacts:
    - path: 'packages/ui/src/button.tsx'
      provides: 'Simplified button with no shadow-xs on outline variant'
      contains: 'outline:'
      not_contains: 'shadow-xs'
    - path: 'packages/ui/src/gold-button.tsx'
      provides: 'Flattened BrandButton without shadow-md, shadow-lg, shadow-primary'
      not_contains: 'shadow-md'
  key_links:
    - from: 'packages/ui/src/button.tsx'
      to: 'outline variant'
      via: 'CVA variants'
      pattern: 'outline:.*(?!shadow)'
---

<objective>
Simplify button components by removing decorative shadows to achieve a minimal, flat aesthetic consistent with Linear/Raycast design.

Purpose: VISL-02 requires simplified button design with less decoration. Currently button.tsx has `shadow-xs` on outline variant, and gold-button.tsx (BrandButton) has heavy shadow decorations including `shadow-md`, `shadow-lg`, and `shadow-primary/25`.

Output: Two button components with flat styling - no shadows, clean hover states with opacity changes only.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-components/04-RESEARCH.md
@packages/ui/src/button.tsx
@packages/ui/src/gold-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shadow from Button outline variant</name>
  <files>packages/ui/src/button.tsx</files>
  <action>
Remove `shadow-xs` from the outline variant in buttonVariants.

Current outline variant (line ~14):

```typescript
outline: 'border-border bg-background hover:bg-muted hover:text-foreground dark:bg-input/30 dark:border-input dark:hover:bg-input/50 aria-expanded:bg-muted aria-expanded:text-foreground shadow-xs',
```

Update to:

```typescript
outline: 'border-border bg-background hover:bg-muted hover:text-foreground dark:bg-input/30 dark:border-input dark:hover:bg-input/50 aria-expanded:bg-muted aria-expanded:text-foreground',
```

Simply remove the trailing ` shadow-xs` from the string.
</action>
<verify>
Run: `grep -n "shadow-xs" packages/ui/src/button.tsx` should return no matches.
Run: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json` should pass.
</verify>
<done>
Button outline variant no longer has shadow-xs class. Other variants unchanged.
</done>
</task>

<task type="auto">
  <name>Task 2: Flatten BrandButton variants</name>
  <files>packages/ui/src/gold-button.tsx</files>
  <action>
Simplify all BrandButton variants by removing shadow classes and heavy effects.

**Default variant** - Remove all shadow classes:

```typescript
// Before
default: [
  'bg-primary',
  'text-primary-foreground',
  'shadow-md shadow-primary/25',
  'hover:bg-primary/90 hover:shadow-lg hover:shadow-primary/30',
  'active:shadow-sm',
],

// After
default: [
  'bg-primary',
  'text-primary-foreground',
  'hover:bg-primary/90',
],
```

**Pulse variant** - Remove shadows and animation (too heavy for minimal aesthetic):

```typescript
// Before
pulse: [
  'bg-primary',
  'text-primary-foreground',
  'shadow-lg shadow-primary/30',
  'animate-pulse',
  'hover:shadow-xl hover:shadow-primary/40',
],

// After - flatten to match default, keep slight scale for feedback
pulse: [
  'bg-primary',
  'text-primary-foreground',
  'hover:bg-primary/90',
],
```

Keep outline and ghost variants as-is (already flat/minimal).
</action>
<verify>
Run: `grep -n "shadow-" packages/ui/src/gold-button.tsx` should return no matches.
Run: `grep -n "animate-pulse" packages/ui/src/gold-button.tsx` should return no matches.
Run: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json` should pass.
</verify>
<done>
BrandButton has flat styling - default and pulse variants no longer have shadow or animation classes. Component maintains functional variants (default, outline, ghost, pulse) but with minimal visual decoration.
</done>
</task>

</tasks>

<verification>
1. No shadow classes in button.tsx: `grep -c "shadow-" packages/ui/src/button.tsx` returns 0
2. No shadow classes in gold-button.tsx: `grep -c "shadow-" packages/ui/src/gold-button.tsx` returns 0
3. TypeScript compiles: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json`
4. Build succeeds: `pnpm build --filter=@sql-pro/ui`
</verification>

<success_criteria>

- Button outline variant has no shadow-xs
- BrandButton has no shadow-md, shadow-lg, shadow-primary, or shadow-xs classes
- BrandButton pulse variant has no animate-pulse
- TypeScript compilation passes
- UI package builds successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/04-core-components/04-01-SUMMARY.md`
</output>
