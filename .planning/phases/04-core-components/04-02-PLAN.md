---
phase: 04-core-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/input.tsx
  - packages/ui/src/textarea.tsx
  - packages/ui/src/select.tsx
autonomous: true

must_haves:
  truths:
    - 'Input fields have no shadow decoration'
    - 'Textarea has no shadow decoration'
    - 'Select trigger has no shadow decoration'
  artifacts:
    - path: 'packages/ui/src/input.tsx'
      provides: 'Input component without shadow-xs'
      not_contains: 'shadow-xs'
    - path: 'packages/ui/src/textarea.tsx'
      provides: 'Textarea component without shadow-xs'
      not_contains: 'shadow-xs'
    - path: 'packages/ui/src/select.tsx'
      provides: 'Select trigger without shadow-xs'
      not_contains: 'shadow-xs'
  key_links:
    - from: 'packages/ui/src/input.tsx'
      to: 'className'
      via: 'cn() merge'
      pattern: 'className.*(?!shadow)'
---

<objective>
Remove decorative shadows from input, textarea, and select components to achieve a minimal, flat form aesthetic.

Purpose: VISL-03 requires refined input/form styling for dark mode. Currently these components have `shadow-xs` which adds unnecessary visual weight. The borders and focus rings provide sufficient feedback without shadows.

Output: Three form input components with flat styling - no shadows, clean borders, proper focus states.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-components/04-RESEARCH.md
@packages/ui/src/input.tsx
@packages/ui/src/textarea.tsx
@packages/ui/src/select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shadow from Input and Textarea</name>
  <files>packages/ui/src/input.tsx, packages/ui/src/textarea.tsx</files>
  <action>
**input.tsx:**
Remove `shadow-xs` from the className string.

Current className includes (line ~12):

```
...text-base shadow-xs transition-[color,box-shadow,background-color]...
```

Update to:

```
...text-base transition-[color,box-shadow,background-color]...
```

Find and remove ` shadow-xs` from the className string.

**textarea.tsx:**
Apply the same change - remove `shadow-xs` from the className string.

Check the file for the pattern and remove ` shadow-xs` from wherever it appears in the className.
</action>
<verify>
Run: `grep -n "shadow-xs" packages/ui/src/input.tsx` should return no matches.
Run: `grep -n "shadow-xs" packages/ui/src/textarea.tsx` should return no matches.
</verify>
<done>
Input and Textarea components no longer have shadow-xs class. They maintain all other styling (borders, focus rings, dark mode backgrounds).
</done>
</task>

<task type="auto">
  <name>Task 2: Remove shadow from Select trigger</name>
  <files>packages/ui/src/select.tsx</files>
  <action>
Remove `shadow-xs` from the SelectTrigger component's className.

Search for `shadow-xs` in the select.tsx file and remove it from wherever it appears in the trigger styling.

Keep all other styling intact (borders, focus rings, chevron positioning).
</action>
<verify>
Run: `grep -n "shadow-xs" packages/ui/src/select.tsx` should return no matches.
Run: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json` should pass.
</verify>
<done>
Select trigger no longer has shadow-xs class. Component maintains all functional styling.
</done>
</task>

</tasks>

<verification>
1. No shadow classes in input.tsx: `grep -c "shadow-xs" packages/ui/src/input.tsx` returns 0
2. No shadow classes in textarea.tsx: `grep -c "shadow-xs" packages/ui/src/textarea.tsx` returns 0
3. No shadow classes in select.tsx: `grep -c "shadow-xs" packages/ui/src/select.tsx` returns 0
4. TypeScript compiles: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json`
</verification>

<success_criteria>

- Input component has no shadow-xs
- Textarea component has no shadow-xs
- Select trigger has no shadow-xs
- All components maintain border, focus ring, and dark mode styling
- TypeScript compilation passes
  </success_criteria>

<output>
After completion, create `.planning/phases/04-core-components/04-02-SUMMARY.md`
</output>
