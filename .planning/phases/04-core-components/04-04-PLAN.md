---
phase: 04-core-components
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/toggle.tsx
  - packages/ui/src/combobox.tsx
  - packages/ui/src/input-otp.tsx
  - packages/ui/src/input-group.tsx
  - packages/ui/src/button-group.tsx
autonomous: true

must_haves:
  truths:
    - 'Toggle has no shadow decoration'
    - 'Combobox input has no shadow decoration'
    - 'All secondary form components have consistent flat styling'
  artifacts:
    - path: 'packages/ui/src/toggle.tsx'
      provides: 'Toggle component without shadow-xs'
      not_contains: 'shadow-xs'
    - path: 'packages/ui/src/combobox.tsx'
      provides: 'Combobox component without shadow-xs'
      not_contains: 'shadow-xs'
  key_links:
    - from: 'packages/ui/src/toggle.tsx'
      to: 'toggleVariants'
      via: 'CVA variants'
      pattern: 'outline:.*(?!shadow)'
---

<objective>
Remove decorative shadows from secondary form components (toggle, combobox, and input-related components) to complete the minimal form aesthetic.

Purpose: VISL-03 requires refined form styling. These secondary components should have the same flat styling as the primary input components for visual consistency.

Output: Five components with flat styling - no shadows, consistent with other form controls.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-components/04-RESEARCH.md
@packages/ui/src/toggle.tsx
@packages/ui/src/combobox.tsx
@packages/ui/src/input-otp.tsx
@packages/ui/src/input-group.tsx
@packages/ui/src/button-group.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shadow from Toggle and Combobox</name>
  <files>packages/ui/src/toggle.tsx, packages/ui/src/combobox.tsx</files>
  <action>
**toggle.tsx:**
Remove `shadow-xs` from the toggleVariants CVA definition, specifically from the outline variant.

Search for `shadow-xs` in the variant definitions and remove it.

**combobox.tsx:**
Remove `shadow-xs` from the combobox input/trigger styling.

Search for `shadow-xs` in the className strings and remove it while preserving all other classes.
</action>
<verify>
Run: `grep -n "shadow-xs" packages/ui/src/toggle.tsx` should return no matches.
Run: `grep -n "shadow-xs" packages/ui/src/combobox.tsx` should return no matches.
</verify>
<done>
Toggle and Combobox components no longer have shadow-xs class. They maintain all other styling.
</done>
</task>

<task type="auto">
  <name>Task 2: Verify and clean secondary input components</name>
  <files>packages/ui/src/input-otp.tsx, packages/ui/src/input-group.tsx, packages/ui/src/button-group.tsx</files>
  <action>
Check these three files for any `shadow-xs` or other shadow classes and remove them if present.

**input-otp.tsx:**
Search for `shadow-xs` or `shadow-` classes and remove if found.

**input-group.tsx:**
Search for `shadow-xs` or `shadow-` classes and remove if found.

**button-group.tsx:**
Search for `shadow-xs` or `shadow-` classes and remove if found.

If no shadow classes are present in any of these files, document that they are already clean.
</action>
<verify>
Run: `grep -n "shadow-xs" packages/ui/src/input-otp.tsx` should return no matches (or file doesn't contain it).
Run: `grep -n "shadow-xs" packages/ui/src/input-group.tsx` should return no matches.
Run: `grep -n "shadow-xs" packages/ui/src/button-group.tsx` should return no matches.
Run: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json` should pass.
</verify>
<done>
All secondary input components have been verified/cleaned. No shadow-xs classes remain in any of these files.
</done>
</task>

</tasks>

<verification>
1. No shadow classes in toggle.tsx: `grep -c "shadow-xs" packages/ui/src/toggle.tsx` returns 0
2. No shadow classes in combobox.tsx: `grep -c "shadow-xs" packages/ui/src/combobox.tsx` returns 0
3. No shadow classes in input-otp.tsx, input-group.tsx, button-group.tsx
4. TypeScript compiles: `pnpm exec tsc --noEmit -p packages/ui/tsconfig.json`
5. Build succeeds: `pnpm build --filter=@sql-pro/ui`
</verification>

<success_criteria>

- Toggle component has no shadow-xs (especially outline variant)
- Combobox has no shadow-xs
- input-otp.tsx, input-group.tsx, button-group.tsx verified clean
- All components maintain border, focus ring, and state styling
- TypeScript compilation passes
- UI package builds successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/04-core-components/04-04-SUMMARY.md`
</output>
